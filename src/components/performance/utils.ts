import type { PerformanceTimeRange } from '@/lib/schema/performance';

// Re-export types for backwards compatibility
export type { PerformanceTimeRange };

// Note: Data fetching functions have been moved to lib/performance/service.ts
// These components now receive data as props from the Performance page
